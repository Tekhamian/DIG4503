
// import Pokemon from 'json-pokemon';

// function response(req, res) {

//     let type = req.query.type;

//     let result = [];

//     //A "for" loop which goes through the entire pokemon database
//     for (let i = 0; i < pokemon.length; i++) {

//         //Another "for" loop which goes through the pokemon type-list
//         for(let j = 0; j < pokemon[i].typeList.length; j++) {
            
//         //*Note: not sure about this process below line of code
//         export default (req, res) => {

//             let result = {"error": "Sorry type not found in database"};
        
//             const type = parseInt(req.query.type);
        
//             let Pokemon = pokemon[i].typeList.length;
        
//             // processs if user answered & Save the results of the pokemon type to an array
//             if(Pokemon !== null) {
//                 let Pokemon = result;
           
//                 const type = parseInt(req.query.type);

//                 let pokemon = getPokemon.getPokemonByType(type);

//         }

//     }
//     res.json(result);
// }



